# Backend Setup (Windows CMD)

**Install &amp; Start:**
```
cd /d "C:/Users/DELL/Desktop/sb-construction-website/backend"
npm install
npm start
```

**PowerShell (if using):**
```
cd "C:/Users/DELL/Desktop/sb-construction-website/backend"
npm install; npm start
```

2. Server runs on http://localhost:3000

3. View leads: http://localhost:3000/leads (JSON)

4. Leads saved to db.json

**Frontend:** Open `index.html` in browser (works standalone, form needs server running).
